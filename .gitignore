# build output — generated by tsup, never commit
dist/

# dependencies
node_modules/

# lockfile — this is a library, not an app; consumers pin their own versions
package-lock.json
yarn.lock
pnpm-lock.yaml

# editor & OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# env
.env
.env.local
